import { type NextPage } from "next";
import Head from "next/head";


const Home: NextPage = () => {
  const createFile = () => {
    const blob = new Blob(['<html>hello world</html>'], {
      type: "text/markdown",
    });    
   const newFile = new File([blob], "Test.md")
   console.log(newFile)
   return newFile
  }

  return (
    <>
      <Head>
        <title>Create T3 App</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="flex min-h-screen flex-col items-center justify-center bg-gradient-to-b from-[#2e026d] to-[#15162c]">
        <div className="container flex flex-col items-center justify-center gap-12 px-4 py-16 ">
          <h1 className="text-5xl  tracking-tight text-white sm:text-[5rem]">
            Create a File test
          </h1>
          <button
        className="rounded-full font-extrabold  bg-white/10 px-10 py-3 font-semibold text-white no-underline transition hover:bg-white/20"
        onClick={() => createFile()}
      >
        Create a file
      </button>
        </div>
      </main>
    </>
  );
};

export default Home;
